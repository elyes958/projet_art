{% extends 'admin/base.html.twig' %}

{% block admin_body %}
    <h4 class="text-center">Consultation d'une catégorie</h4>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ categorie.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categorie_index') }}">Retourner à la liste</a>

    <a href="{{ path('categorie_edit', {'id': categorie.id}) }}">Modifier</a>

    {{ include('admin/categorie/_delete_form.html.twig') }}
{% endblock %}
